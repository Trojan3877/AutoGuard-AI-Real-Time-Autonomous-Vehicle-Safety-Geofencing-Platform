apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "autoguard.serviceAccountName" . }}
  labels:
    {{- include "autoguard.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
