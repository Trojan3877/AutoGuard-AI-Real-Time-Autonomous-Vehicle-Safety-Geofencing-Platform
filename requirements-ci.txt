# Lightweight dependency set used by CI (lint + unit tests).
# Heavy ML/GPU packages (torch, torchvision, stable-baselines3, gymnasium,
# opencv-python-headless, snowflake-connector-python) are intentionally
# excluded so CI runs in a reasonable time without GPU hardware.

# API / backend
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Data & streaming (CPU-only subset)
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.13.0

# Observability
prometheus-client>=0.20.0

# HTTP (used by geofence check + dashboard)
requests>=2.31.0

# Testing
pytest>=8.1.0
coverage>=7.4.0
httpx>=0.27.0
